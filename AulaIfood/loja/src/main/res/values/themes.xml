<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.AulaIfood" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/primaria</item>
        <item name="android:windowBackground">@color/branco</item>
        <item name="android:statusBarColor">@color/branco</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>

    <style name="Theme.AulaIfood" parent="Base.Theme.AulaIfood" />
</resources>